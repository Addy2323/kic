import { ArrowUpRight } from 'lucide-react'
import { PageFrame, PageHero } from '@/components/site-shell'
import { newsItems } from '@/lib/content'
export const metadata = { title: 'News & Insights | KIC Ltd', description: 'Perspectives and updates from KIC Ltd.' }
export default function NewsPage() { return <PageFrame><PageHero eyebrow="News & insights" title="Thinking in public." intro="Perspectives on the sectors, opportunities and communities shaping our work." /><section className="mx-auto max-w-7xl px-6 py-24 lg:px-10 lg:py-36"><div className="divide-y divide-border border-y border-border">{newsItems.map(item => <article key={item.title} className="group grid gap-8 py-10 lg:grid-cols-[.35fr_1fr_1fr] lg:items-start"><div><p className="eyebrow text-accent">{item.category}</p><p className="mt-3 text-sm text-muted-foreground">{item.date}</p></div><h2 className="text-3xl font-medium tracking-tight group-hover:text-accent">{item.title}</h2><div className="flex items-start justify-between gap-5"><p className="max-w-md leading-relaxed text-muted-foreground">{item.text}</p><ArrowUpRight size={20} className="shrink-0 transition-transform group-hover:translate-x-1 group-hover:-translate-y-1" /></div></article>)}</div></section></PageFrame> }
