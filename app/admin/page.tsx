'use client'
import { useState } from 'react'
import { adminSections } from '@/lib/content'
export default function AdminPage() { const [active, setActive] = useState(adminSections[0]); return <main className="min-h-screen bg-secondary text-foreground"><header className="flex items-center justify-between border-b border-border bg-background px-6 py-5 lg:px-10"><div className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center bg-primary text-sm font-semibold text-primary-foreground">KIC</span><span className="text-xs font-semibold uppercase tracking-[.2em]">Content workspace</span></div><span className="text-xs uppercase tracking-[.15em] text-muted-foreground">Local preview · no persistence</span></header><div className="mx-auto grid max-w-7xl gap-8 px-6 py-8 lg:grid-cols-[240px_1fr] lg:px-10 lg:py-12"><aside className="border-b border-border pb-6 lg:border-b-0 lg:border-r lg:pr-6"><p className="eyebrow mb-5 text-muted-foreground">Manage</p><nav className="grid gap-1">{adminSections.map(section => <button key={section} onClick={() => setActive(section)} className={`rounded-none px-3 py-3 text-left text-sm transition-colors hover:bg-background ${active === section ? 'bg-background font-semibold text-accent' : 'text-muted-foreground'}`}>{section}</button>)}</nav></aside><section className="bg-background p-6 lg:p-10"><p className="eyebrow text-accent">Editable content</p><h1 className="mt-4 text-4xl font-medium tracking-tight">{active}</h1><p className="mt-5 max-w-xl leading-relaxed text-muted-foreground">This CMS-ready workspace is the editorial surface for {active.toLowerCase()}. Connect a database and authentication layer when the team is ready to persist changes securely.</p><div className="mt-10 grid gap-4 sm:grid-cols-2"><div className="border border-border p-5"><p className="text-sm font-semibold">Content status</p><p className="mt-3 text-sm text-muted-foreground">Draft workspace</p></div><div className="border border-border p-5"><p className="text-sm font-semibold">Publishing</p><p className="mt-3 text-sm text-muted-foreground">Preview only until a backend is connected</p></div></div><textarea aria-label={`${active} draft`} defaultValue={`Update ${active.toLowerCase()} content here...`} className="mt-8 min-h-48 w-full resize-y border border-border bg-secondary p-4 font-mono text-sm outline-none focus:border-accent" /></section></div></main> }
